# Studio UI 全面精修实施计划（v1.0.7）

## Task 1：建立契约与分支基线

- 在 `codex/studio-ui-polish-v1.0.7` 上记录设计、验收标准和基线。
- 为单图完整展示、侧栏列表结构、顶部更多菜单、生成设置合并和结果操作分层增加 RED 测试。
- 基线：16 个 Node 模块 124/124，TypeScript/Vite build 通过。

## Task 2：结果优先与内容对齐

- 单图默认展开，按真实宽高比完整展示。
- 多图保留折叠/展开，展开状态不裁图。
- 结果操作改为三项可见主操作 + 更多菜单。
- 会话流和输入区建立共同最大宽度与对齐边界。

## Task 3：控制减负与侧栏修正

- 包装侧栏计数和列表，移除重复新对话入口。
- 顶部移除保存，新增更多菜单与明确的清空当前对话。
- 合并尺寸、质量、数量为生成设置弹层。
- 澄清队列、会话本地状态和提示词帮助文案。

## Task 4：系统化视觉精修

- 收紧圆角 token 和普通按钮半径。
- 保留切换器/状态的胶囊语义，减少黑色主动作竞争。
- 简化队列玻璃效果，统一结果动作、菜单和聚焦状态。
- 检查桌面、平板、手机和减少动态效果样式。

## Task 5：验证与交接

- 跑 16 个 Node 模块、`npm run test:size`、TypeScript/Vite build、发布/缓存门禁。
- 用当前 `14260` 服务做 health、index、JS、CSS HTTP smoke，不调用付费上游。
- 更新 `PROJECT_STATUS.md`、`NEXT_ACTIONS.md`、`DECISIONS.md`，提交小步产品和文档 commit。
- 真人浏览器确认前不打包、不写 G:、不推送、不合并、不发布。

